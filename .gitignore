# Python
venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Data and raw files
raw_data/
*.nii.gz
*.dcm
*.pkl
*.tmp

# Preprocessed data — ignore bulk pickles, keep split + summary committed
data/pickles/
!data/split.csv
!data/preprocessing_summary.csv
data/zotero_library.json
data/zotero_papers.json

# Curated results (figures + summary) are committed under results/
# Everything under outputs/ stays gitignored

# Training outputs — checkpoints, logs and visualisations are too large / ephemeral
outputs/checkpoints_*/
outputs/visualisations/
outputs/logs/

# Evaluation — ignore stale/archived runs; track current valid CSVs
outputs/evaluation/baseline_sigmoid/
outputs/evaluation/archived_ablations/

# Analysis — ignore archived partial runs; track current figures and CSVs
outputs/analysis/archive/

# Ignore large checkpoint, log, and visualisation dirs entirely
outputs/checkpoints_*/
outputs/visualisations/
outputs/logs/

# Collaborator reference files — private HPC paths, not part of this thesis
nika/

# Personal context files — ignore everything in docs/ except the tracker
docs/*
!docs/thesis_reporting_tracker.md
!docs/literature_comparison.md
!docs/results_validation.md
thesis_template
CLAUDE.md
.claude/
*.zip

# W&B
wandb/

# SLURM — ignore logs; track job scripts as part of the pipeline
*.out
*.err
